"""Streamlit pages declared in ``app.py`` via ``st.Page``.

Each module here is a thin shell over ``core.api`` — widgets, session
state and rendering only. Engineering and optimisation logic must
not appear in this package.
"""
