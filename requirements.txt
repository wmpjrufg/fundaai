# =============================================================================
# FundaIA — runtime requirements
# Codificação: UTF-8 (sem BOM). Sintaxe pip padrão.
# Pinos preservados do snapshot de produção; atualizar com cuidado.
#
# Dependências de notebooks (jupyter) e auxiliares de manutenção (pip-chill)
# permanecem aqui por compatibilidade com o fluxo atual da equipe.
# Dependências de operação (playwright) seguem isoladas em ops/requirements.txt.
# =============================================================================

# --- Núcleo numérico / científico --------------------------------------------
numpy
pandas
scipy
matplotlib
joblib
scikit-learn==1.7.2

# --- Otimização ---------------------------------------------------------------
mealpy==3.0.3

# --- Interface web (Streamlit) -----------------------------------------------
streamlit==1.52.1
# Plotly powers the interactive 3D footings viewer (frontend/components/
# footings_3d.py); rendered via st.plotly_chart so the user can rotate,
# zoom and toggle traces in the browser.
plotly>=5,<7

# --- I/O ----------------------------------------------------------------------
openpyxl==3.1.5
xlsxwriter==3.2.9
ezdxf==1.4.3
# Parquet engine for experiment history persistence (core/io/experiments.py).
pyarrow>=14.0,<24.0

# --- Configuration validation -------------------------------------------------
# OptimisationConfig (core/api/types.py) is a Pydantic v2 BaseModel.
pydantic>=2.0,<3.0

# --- Notebooks (dev) ----------------------------------------------------------
jupyter==1.1.1

# --- Auxiliares (transitivas explicitamente preservadas do snapshot) ---------
fqdn==1.5.1
isoduration==20.11.0
jsonpointer==3.0.0
rfc3987-syntax==1.1.0
tinycss2==1.4.0
uri-template==1.3.0
webcolors==25.10.0

# --- Utilidades de manutenção -------------------------------------------------
pip-chill==1.0.3
