"""Domain layer — business entities of the foundation design problem.

This subpackage will host immutable, framework-free dataclasses that
represent the project entities (Solo, Pilar, Combinacao, Sapata,
FundacaoProjeto). It depends on nothing else from ``core`` and on no
external library beyond the Python standard library and (eventually)
``pydantic`` for validation.

Resumo em português:
    Camada de domínio. Reúne as entidades de negócio (Solo, Pilar,
    Combinação, Sapata, FundacaoProjeto) como dataclasses imutáveis,
    sem dependência de frameworks de UI ou bibliotecas pesadas.
"""
