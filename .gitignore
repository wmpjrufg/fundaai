*.txt
*.pyc
/__pycache__/
/ambiente_fundaIA/
!requirements.txt
env/
venv/
myenv/
.venv/
.vscode/
.idea/
.DS_Store
# Estado local do Obsidian aberto na raiz e backups locais de fonte.
.obsidian/
*.bak

# --- Entrega para o orientador (2026-07-24) ----------------------------------
# `docs/` (artigos, plano de IC, relatorios, artigo LaTeX), `obsidian_vault/`
# (vault Obsidian) e `experiments/` (runs persistidos) passam a ser versionados
# a pedido, para revisao completa pelo orientador. `exports/` permanece local.
/exports/


# --- Configuracao local do Claude Code (nao versionado) ----------------------
# Settings locais por maquina; nao deve entrar no historico publico.
/.claude/

# --- Modelos GPR persistidos (nao versionado) --------------------------------
# Reproduziveis a partir de fundacao.aprendizado_maquina_paralelo (~553 MB).
# Manter no historico do repo seria pesado e desnecessario.
/models/
*.pkl

# --- Experimentos persistidos: versionados para a entrega ao orientador ------
# Pastas geradas pelo ExperimentRecorder (Sprint 4.2) + o teste de 25 sapatas.
# Antes ignoradas (/experiments/); agora incluidas a pedido para revisao.

# --- Notebooks scratch (nao versionado) --------------------------------------
# Subpasta livre para esboços e exploração rápida; conteúdo "abençoado"
# vai direto em notebooks/.
/notebooks/scratch/